#include <stdio.h>
#include <stdlib.h>
#include "Tree.h"

int main()
{
    Tree *father = NULL;
    char stringTree[50] = "ENGENHARIA DE SOFTWARE";



    return 0;
}
/*
    Tree *father = NULL;
    char *arvore;
    arvore = "532..4..8.9..";

    father = buildsTree(arvore);
    printTree(father);

    int sizeTree = countLeafs(father);
    int height = heightTree(father);

    printf("\nQuantidade: %d\tNivel: %d\t\n",sizeTree,height);

    Tree *root = NULL;
    root = searchValue('2', father);

    printTree(root);
    //root = insertTree('7', father);
*/
